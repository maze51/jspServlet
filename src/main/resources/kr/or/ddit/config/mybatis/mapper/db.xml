<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="db">
	<!-- 전체 URI Mapping 리스트 조회 -->
	<select id="mappingList" resultType="dbVo">
		select * from urimapping
	</select>
</mapper>